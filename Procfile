# Heroku Procfile for DB CLI
# This service is designed to run as scheduled jobs via Heroku Scheduler
# No web process needed - use Heroku Scheduler to run commands like:
# python app.py schedule sync --date 2024-01-15
# python app.py live monitor --game-id 2023020001

